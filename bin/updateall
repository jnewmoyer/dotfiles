#! /bin/bash

h=${GOPATH}/src/github.com/Novetta
echo "$h"
cd ${h}
for d in $(/bin/ls -d ./*); do 
	echo "$d" 
	cd "${h}/$d" 
	if [ -d ".git" ]; then
		git pull 
		subupdate 
		prune-branches 
	fi
	cd ${h}
done
