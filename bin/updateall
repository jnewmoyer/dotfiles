#! /bin/bash

h=${GOPATH}/src/github.com/Novetta
cd ${h}
for dir in $(/bin/ls -d ./*); do 
	if [ -d ".git" ]; then
		echo "$dir" 
		cd "${h}/$dir" 
		git pull 
		subupdate 
		prune-branches 
	fi
done
